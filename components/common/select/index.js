// const Select = ({ label, name, onChange, disabled }) => (
//     <label htmlFor={label + '-' + name} className={`checkbox`}>
//         <input name={name} onChange={onChange} type="checkbox" id={label + '-' + name} disabled={disabled} />
//         <span className="checkbox__check"></span>
//         <p>{label}</p>
//     </label>
// );

// export default Select;